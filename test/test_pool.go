package test

func Test_pool() {
	// h := core.NewTxPool().Heap
	// heap.Push(&h, &core.Transaction{GasFee: big.NewInt(33)})
	// heap.Push(&h, &core.Transaction{GasFee: big.NewInt(3)})
	// heap.Push(&h, &core.Transaction{GasFee: big.NewInt(4)})
	// heap.Push(&h, &core.Transaction{GasFee: big.NewInt(11)})
	// for len(h) > 0 {
	// 	fmt.Printf("%v\n", heap.Pop(&h))
	// }
}
